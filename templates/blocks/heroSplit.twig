{% extends 'blocks/__base__' %}

{% block block_wrapper %}
    {% set image = item.image.one() %}
    {% set splitStyle = item.splitStyle %}
    {% switch item.splitStyle %}
    {% case '6040' %}
        {% set parentCols = 'lg:grid-cols-5' %}
        {% set leftSide = 'lg:col-span-3' %}
        {% set rightSide = 'lg:col-span-2' %}
    {% case '4060' %}
        {% set parentCols = 'lg:grid-cols-5' %}
        {% set leftSide = 'lg:col-span-2' %}
        {% set rightSide = 'lg:col-span-3' %}
    {% case '7525' %}
        {% set parentCols = 'lg:grid-cols-4' %}
        {% set leftSide = 'lg:col-span-3' %}
        {% set rightSide = 'lg:col-span-1' %}
    {% case '2575' %}
        {% set parentCols = 'lg:grid-cols-4' %}
        {% set leftSide = 'lg:col-span-1' %}
        {% set rightSide = 'lg:col-span-3' %}
    {% default %}
        {% set parentCols = 'lg:grid-cols-4' %}
        {% set leftSide = 'lg:col-span-2' %}
        {% set rightSide = 'lg:col-span-2' %}
    {% endswitch %}

    <div class="gs-group relative grid w-full grid-cols-1 grid-rows-1 lorge:min-h-screen {{
        parentCols
        }} bg-neutral-900">
        <div class="block-wrapper col-span-1 flex flex-col items-start justify-center px-8 {{
            leftSide
            }}">
            <div class="flex w-full flex-col items-start justify-end self-end lorge:max-w-[80%] yuge:max-w-[60%]">
                {% if item.header %}
                    <h1 class="gs-lazy mb-8 lg:mb-12">
                        {{ item.header }}
                    </h1>
                {% endif %}
                <div class="gs-lazy gs-content cms-content text-lg leading-loose tracking-wide xl:columns-2 xl:gap-16">
                    {{ item.body|raw }}
                </div>
            </div>
        </div>

        {% if image %}
            <div class="relative col-span-1 {{ rightSide }} max-lg:h-96">
                {% include 'components/background-image' with {
                    image: image
                } %}
            </div>
        {% endif %}
    </div>
{% endblock %}
